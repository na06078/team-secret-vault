# 📘 배포 안내서 — 서버를 켜서 인터넷에 공개하기

이 서비스는 **서버를 켜는 사람(운영자) 1명**과 **접속해서 쓰는 사람(사용자) 여러 명**으로 나뉩니다.

- **사용자**: 아무것도 설치할 필요 없습니다. 운영자가 알려준 주소를 브라우저로 열어 회원가입하고 쓰면 끝입니다.
- **운영자**: 아래 순서대로 자기 컴퓨터(또는 서버)에 프로그램을 준비하고 서버를 켜면 됩니다.

이 문서는 **운영자**를 위한 것입니다.

---

## 0. 준비물 (한 번만 설치)

이 저장소를 처음 받은 컴퓨터라면 아래를 설치합니다. (이미 있으면 건너뛰기)

### ① Python 3.11 이상
- https://www.python.org/downloads/ 에서 설치. 설치 시 "Add Python to PATH" 체크.

### ② 코드 내려받기
```bash
git clone https://github.com/na06078/team-secret-vault.git
cd team-secret-vault
```

### ③ 파이썬 패키지 설치 (가상환경)
```bash
python -m venv .venv
source .venv/Scripts/activate      # Windows(git-bash)
# macOS/Linux는:  source .venv/bin/activate
pip install -r backend/requirements.txt
```
> `requirements.txt`에 Flask·waitress·bcrypt 등 필요한 것이 다 적혀 있어서, 이 한 줄로 자동 설치됩니다.

### ④ cloudflared (인터넷 공개용 터널 프로그램)
파이썬 패키지가 아니라 별도 프로그램이라 따로 설치합니다.
```bash
# Windows
winget install --id Cloudflare.cloudflared
# macOS
brew install cloudflared
# Linux: https://github.com/cloudflare/cloudflared/releases 에서 받기
```
> 인터넷 공개를 안 하고 내 컴퓨터에서만 쓸 거라면 cloudflared는 필요 없습니다.

### ⑤ 세션키 설정 (권장)
로그인 상태를 유지하는 서명키입니다. 안 하면 서버 재시작 때마다 로그인이 풀립니다.
```bash
python -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))" > .env
```

---

## 1. 내 컴퓨터에서만 써보기 (인터넷 공개 없이)

```bash
cd backend && python app.py
#   → 브라우저에서 http://127.0.0.1:5000
```
같은 컴퓨터에서만 접속됩니다. 기능 확인용.

---

## 2. 인터넷에 공개하기 (다른 사람도 접속)

**터미널 2개**를 씁니다. Cloudflare Tunnel을 쓰기 때문에 도메인 구입도, 공유기 포트 개방도 필요 없습니다.

> 원리: 내 컴퓨터 안에서 서버(`localhost:5000`)를 돌리고, cloudflared가 그 서버를
> `https://랜덤이름.trycloudflare.com` 공개 주소로 안전하게 연결해 줍니다.
> 공유기 포트를 열지 않으므로 내 네트워크가 인터넷에 직접 노출되지 않습니다.

### 터미널 ① — 서버 실행 (배포용)
```bash
source .venv/Scripts/activate
python backend/서버실행.py
#   → http://127.0.0.1:5000 에서 waitress 서버가 뜸 (이 창은 켜둔 채로)
```

### 터미널 ② — 터널 열기
```bash
cloudflared tunnel --url http://127.0.0.1:5000
```
- 명령이 안 잡히면 전체 경로로 (Windows 예):
  ```bash
  "/c/Program Files (x86)/cloudflared/cloudflared.exe" tunnel --url http://127.0.0.1:5000
  ```
- 실행하면 로그 중간에 공개 주소가 나옵니다:
  ```
  +----------------------------------------------------+
  |  https://랜덤이름.trycloudflare.com                 |
  +----------------------------------------------------+
  ```
- **이 주소를 사람들에게 알려주면 접속·회원가입해서 씁니다.** (HTTPS 정상, 경고 없음)

### 끄기
- 두 터미널에서 각각 **Ctrl + C**. 터널을 끄면 공개 주소도 사라집니다.

---

## 3. 알아둘 점

- **주소가 매번 바뀝니다.** 기본(Quick Tunnel)은 서버를 껐다 켤 때마다 새 랜덤 주소를 줍니다. 잠깐 시연·테스트용으로 충분합니다.
- **고정 주소 + 원하는 이름**을 쓰려면: 무료 **Cloudflare 계정** + **도메인 하나**를 등록해 "Named Tunnel"로 설정하면 `secret.내도메인.com` 같은 고정 주소를 쓸 수 있습니다. (도메인은 무료 `.eu.org` 등도 가능하고, 유료는 연 1~2만원)
- **내 컴퓨터가 꺼지면 서비스도 꺼집니다.** 24시간 공개하려면 컴퓨터를 계속 켜두거나, 클라우드 서버(VPS)에 이 코드를 올려서 돌리면 됩니다. 코드는 그대로라 어디서 돌리든 동일합니다.
- **HTTPS가 필요한 이유**: 이 서비스의 암호화(브라우저 WebCrypto)는 `https://` 또는 `localhost`에서만 동작합니다. Cloudflare Tunnel이 HTTPS를 자동으로 붙여주므로 문제없습니다. (그냥 `http://내IP:5000`으로 열면 암호화가 꺼져 로그인·저장이 안 됩니다.)
- **데이터 위치**: 사용자·비밀 데이터는 서버 컴퓨터의 `backend/instance/금고.db` 파일에 저장됩니다. 이 파일이 전체 데이터이므로 백업해두면 안전합니다.

---

## 개발용 vs 배포용 서버

| | 명령 | 용도 |
|---|---|---|
| 개발용 | `python backend/app.py` | 코드 고칠 때. 자동 새로고침·디버그 |
| 배포용 | `python backend/서버실행.py` | 실제로 켜둘 때. waitress(안정적) |
