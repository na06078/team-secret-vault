# 📘 배포 안내서 — 내 PC로 인터넷에 공개하기

내 PC를 서버로 삼아, 다른 사람도 인터넷으로 접속할 수 있게 하는 방법입니다.
**Cloudflare Tunnel**을 쓰기 때문에 도메인 구입도, 공유기 포트 개방도 필요 없습니다.

> 원리: 내 PC 안에서 서버(`localhost:5000`)를 돌리고, cloudflared가 그 서버에 붙어
> `https://랜덤이름.trycloudflare.com` 같은 공개 주소로 안전하게 연결해 줍니다.
> 공유기 포트를 열지 않으므로 내 집 네트워크가 인터넷에 노출되지 않습니다.

---

## 미리 설치된 것 (이미 완료됨)

- `waitress` — 배포용 서버 (venv 안에 설치됨)
- `cloudflared` — Cloudflare Tunnel 프로그램 (winget으로 설치됨)

새 PC에서 처음 세팅한다면 아래로 설치:
```bash
# venv 만들고 패키지 설치
python -m venv .venv
source .venv/Scripts/activate
pip install -r backend/requirements.txt

# cloudflared 설치 (Windows)
winget install --id Cloudflare.cloudflared
```

---

## 켜는 방법 (터미널 2개)

### 터미널 ① — 서버 실행
```bash
cd "C:\my vault\ME\1. Note\Hermes\Aiffel_Work\팀비밀금고"
source .venv/Scripts/activate
python backend/서버실행.py
#   → http://127.0.0.1:5000 에서 waitress 서버가 뜸 (이 창은 켜둔 채로)
```

### 터미널 ② — 터널 열기
```bash
cloudflared tunnel --url http://127.0.0.1:5000
```
- cloudflared가 설치돼도 `cloudflared` 명령이 안 잡히면 전체 경로로:
  ```bash
  "/c/Program Files (x86)/cloudflared/cloudflared.exe" tunnel --url http://127.0.0.1:5000
  ```
- 실행하면 로그 중간에 이런 줄이 나옵니다:
  ```
  +----------------------------------------------------+
  |  https://랜덤이름.trycloudflare.com                 |
  +----------------------------------------------------+
  ```
- **이 주소를 다른 사람에게 알려주면 접속됩니다.** (HTTPS 자물쇠 정상, 경고 없음)

---

## 끄는 방법
- 두 터미널에서 각각 **Ctrl + C**. 터널을 끄면 공개 주소도 사라집니다.

---

## 알아둘 점

- **주소는 켤 때마다 바뀝니다.** Quick Tunnel(무료·계정 불필요)은 실행할 때마다 새 랜덤 주소를 줍니다. 테스트·시연용으로 충분합니다.
- **고정 주소가 필요하면**: 무료 Cloudflare 계정 + (무료여도 되는) 도메인을 등록해 "Named Tunnel"로 올리면 `secret.내도메인.com` 같은 고정 주소를 쓸 수 있습니다. 필요할 때 안내 가능.
- **내 PC가 꺼지면 서비스도 꺼집니다.** 24시간 공개하려면 PC를 계속 켜두거나, VPS(클라우드 서버)에 올려야 합니다. 소스 코드는 그대로라 어디서 돌리든 동일합니다.
- **HTTPS가 반드시 필요한 이유**: 이 서비스의 암호화(브라우저 WebCrypto)는 `https://` 또는 `localhost`에서만 동작합니다. Cloudflare Tunnel은 정식 HTTPS를 자동으로 붙여주므로 문제없습니다. (그냥 `http://내IP:5000`으로 열면 암호화가 꺼져 로그인·저장이 안 됩니다.)
- **데이터 위치**: 사용자·비밀 데이터는 내 PC의 `backend/instance/금고.db`에 저장됩니다. 이 파일이 곧 전체 데이터이므로 백업해두면 안전합니다.

---

## 개발용 vs 배포용 서버

| | 명령 | 용도 |
|---|---|---|
| 개발용 | `python backend/app.py` | 코드 고칠 때. 자동 새로고침·디버그 |
| 배포용 | `python backend/서버실행.py` | 실제로 켜둘 때. waitress(안정적) |
