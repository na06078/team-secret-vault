# 📘 복구 키(Recovery Key) 기능 명세서 — 팀 비밀 금고 v2

> **작성 목적**: 이 문서만 읽고도 복구 키 기능을 기존 제로지식 설계와 어긋남 없이 구현할 수 있게 하는 명세서.
> **상태**: ✅ 구현 완료·검증 통과 (2026-09-06). 아래는 구현된 설계의 명세.
> **전제**: 기존 `📘팀비밀금고_PRD.md`의 보안 모델을 이미 구현·검증한 상태에서, 그 위에 얹는 증분(v2) 설계다.

---

## 1. 왜 필요한가 (배경과 트레이드오프)

### 1.1 현재 문제
현재 v1은 **마스터 비번을 잊으면 금고를 영영 못 연다.** 개인키는 오직 마스터 비번에서 유도한 `vaultKey`로만 잠겨 있고, 서버에도 사용자에게도 다른 열쇠가 없기 때문이다. 이는 제로지식의 의도된 특성이지만, 실사용에서는 "비번 한 번 잊으면 모든 시크릿 소멸"이라는 치명적 불편이 된다.

### 1.2 이 기능이 하는 일
가입 시 **길고 무작위한 복구 키(Recovery Key)**를 딱 한 번 발급한다. 사용자는 이걸 종이·비밀번호 관리자 등 안전한 곳에 보관한다. 마스터 비번을 잊으면, 복구 키로 개인키를 되살리고 **새 마스터 비번**을 설정한다. (Bitwarden의 Recovery Code와 동일한 개념)

### 1.3 트레이드오프 (이 실습의 핵심 학습 포인트)
복구 경로가 하나 늘어난다는 것은 **공격 표면도 하나 늘어난다**는 뜻이다.
- **얻는 것**: 비번 분실 시 복구 가능 → 실사용성 대폭 상승.
- **잃는 것**: 복구 키가 유출되면 그 자체로 계정이 뚫린다(마스터 비번과 동급의 비밀). 즉 "기억해야 할 비밀"이 하나에서 둘로 늘고, 둘 중 **더 약한 쪽**이 계정의 실제 보안 수준이 된다.
- **완화**: 복구 키는 사람이 외우는 게 아니라 **256비트 무작위**로 생성 → 무차별 대입이 사실상 불가능. 대신 "물리적 보관"의 책임이 사용자에게 넘어간다.

> 핵심 교훈: **보안을 높이는 기능(제로지식)과 복구성을 높이는 기능(복구 키)은 서로 당긴다.** 이 긴장 관계를 눈으로 확인하는 것이 이 기능의 학습 목표다.

---

## 2. 설계 원칙

1. **제로지식 불변**: 복구 키도 **서버로 원본이 가지 않는다.** 기존 마스터 비번과 완전히 대칭인 "두 갈래 유도" 구조를 그대로 재사용한다.
2. **개인키 이중 봉인**: 같은 RSA 개인키를, ① 마스터 비번 유도 키(`vaultKey`)와 ② 복구 키 유도 키(`recoveryWrapKey`)로 **각각 따로 봉인**해 두 사본을 서버에 저장한다. 어느 쪽 열쇠로도 개인키를 되살릴 수 있다.
3. **복구 키도 해시로만 검증**: 복구 요청을 서버가 인가할 때, 복구 키 원본이 아니라 그로부터 유도한 `recoveryAuthKey`를 **bcrypt 해시**로 대조한다(로그인의 `authKey`와 동일 방식). → 배운 "해시/솔트"가 한 번 더 정직하게 쓰인다.
4. **복구 = 비번 재설정**: 복구에 성공하면 개인키만 되살리는 게 아니라, **새 마스터 비번을 즉시 설정**한다(새 salt·authKey·enc_priv 갱신). 옛 비번은 폐기된다.
5. **일회 표시**: 복구 키 평문은 **발급 순간 화면에 딱 한 번**만 보여주고 어디에도 저장하지 않는다. 분실 시 재발급(로그인 상태에서)만 가능.

---

## 3. 보안 모델 (기존과 대칭 구조)

### 3.1 회원가입 시 (변경: 복구 키 봉인 추가)
1. 기존대로 마스터 비번 `P` → `salt` → `authKey`(서버로), `vaultKey`(로컬).
2. 기존대로 RSA 키쌍 생성. 개인키를 `vaultKey`로 봉인 → `encPriv`.
3. **[신규]** 클라이언트가 **복구 키 `R`**(256비트 무작위)를 생성하고, 별도 `recoverySalt`로 PBKDF2 2갈래 유도:
   - **`recoveryAuthKey`**: 서버로 감 → 서버가 bcrypt 해시(`recovery_hash`)로 저장. (복구 요청 인가용)
   - **`recoveryWrapKey`**: 로컬 전용. 개인키를 이걸로 **한 벌 더 봉인** → `encPrivRecovery`.
4. 서버로 전송(기존 필드 + 신규): `... , recovery_salt, recovery_hash용 recoveryAuthKey, encPrivRecovery, encPrivRecovery_iv`.
5. **복구 키 `R`을 사람이 읽을 형식으로 화면에 1회 표시** (예: `XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX`). 사용자가 "저장했음"을 체크해야 다음 단계.

### 3.2 계정 복구(비번 재설정) 시
1. 사용자가 `username` + 복구 키 `R` 입력.
2. 서버에서 `recovery_salt`를 받아옴. (전용 엔드포인트)
3. 클라이언트가 `R`+`recoverySalt`로 `recoveryAuthKey`·`recoveryWrapKey` 재유도.
4. `recoveryAuthKey`를 서버로 전송 → 서버가 bcrypt로 `recovery_hash`와 대조. 실패 시 거부.
5. 성공 시 서버가 `encPrivRecovery`(+iv) 반환 → 클라이언트가 `recoveryWrapKey`로 **개인키 복원**.
   - (AES-GCM 인증 태그 덕분에, 복구 키가 틀리면 이 단계에서 복호화 자체가 실패한다 → 이중 안전장치.)
6. 사용자가 **새 마스터 비번 `P'`** 입력 → 새 `salt'`·`authKey'`·`vaultKey'` 유도 → 복원된 개인키를 `vaultKey'`로 재봉인 → `encPriv'`.
7. 서버로 전송: `username, salt', authKey'(→bcrypt), encPriv', encPriv'_iv`. 서버가 users 레코드 갱신.
   - **복구 키 사본은 그대로 유지**(재발급 안 하면 같은 복구 키로 다음에도 복구 가능). 공개키·시크릿·공유는 **전부 그대로 유효**(개인키가 바뀌지 않았으므로).

### 3.3 복구 키 재발급 시 (로그인 상태, 선택 기능)
1. 로그인되어 개인키가 메모리에 있는 상태에서 요청.
2. 새 복구 키 `R2` 생성 → 3.1의 3~4단계를 다시 수행해 `recovery_salt`, `recovery_hash`, `encPrivRecovery`를 **덮어쓴다**.
3. `R2`를 1회 표시. 옛 복구 키는 무효화.

### 3.4 개념 매핑 (배운 것이 또 어떻게 쓰이나)
| 배운 개념 | 복구 키 기능에서 하는 일 |
|---|---|
| 해시/솔트 | 복구 키에서 유도한 `recoveryAuthKey`를 bcrypt로 저장 → 복구 요청 인가에 사용 |
| 공개키/개인키 | 되살리는 대상이 바로 RSA 개인키. 복구해도 공개키는 그대로라 기존 공유·시크릿 유효 |
| 세션키(대칭키) | 개인키를 봉인하는 `recoveryWrapKey`가 AES-GCM 대칭키 |
| KDF(PBKDF2) | 마스터 비번과 동일하게, 복구 키도 2갈래로 유도 |

### 3.5 위협 모델 갱신
- ✅ **여전히 막음**: 서버 유출 시에도 `encPriv`, `encPrivRecovery` 둘 다 암호문이라 못 엶. `recovery_hash`는 bcrypt라 복구 키 역산 불가.
- ⚠️ **새 위험**: 복구 키 평문이 유출되면 = 마스터 비번 유출과 동급(계정 탈취 가능). → 사용자에게 "복구 키는 마스터 비번만큼 안전하게 보관" 경고 필수.
- ⚠️ **비번 재설정 남용**: 복구 엔드포인트는 `recovery_hash` 검증을 통과해야만 동작하므로, 복구 키를 모르는 공격자는 재설정도 못 한다(3.2의 4단계가 방어선). bcrypt 대조 전에는 `encPrivRecovery`를 반환하지 않는다.

---

## 4. 데이터 모델 변경

`users` 테이블에 컬럼 4개 추가. (기존 컬럼은 그대로)

```sql
ALTER TABLE users ADD COLUMN recovery_salt          TEXT;  -- base64, 복구키 KDF용
ALTER TABLE users ADD COLUMN recovery_hash          TEXT;  -- bcrypt(recoveryAuthKey), 복구 요청 인가용
ALTER TABLE users ADD COLUMN enc_priv_recovery      TEXT;  -- base64, recoveryWrapKey로 봉인한 개인키(두 번째 사본)
ALTER TABLE users ADD COLUMN enc_priv_recovery_iv   TEXT;  -- base64, 그 봉인의 IV
```

- **NULL 허용**: 복구 키 기능 이전에 가입한 사용자(마이그레이션 대상)는 이 값들이 NULL → "복구 키 미설정" 상태. 로그인 후 3.3 흐름으로 설정하게 유도.
- `schema.sql`은 신규 설치용으로 위 컬럼을 `CREATE TABLE`에 직접 포함하고, 기존 DB는 `db.py`의 `init_db()`에서 컬럼 존재 여부를 확인해 없으면 `ALTER TABLE`로 추가(단순 마이그레이션).

---

## 5. API 계약 (신규/변경)

### 5.1 회원가입 (변경) — `POST /api/register`
- **입력(추가)**: 기존 필드 + `recovery_salt, recovery_auth_key, enc_priv_recovery, enc_priv_recovery_iv`
- **처리(추가)**: `recovery_hash = bcrypt(recovery_auth_key)` 저장.
- **하위호환**: 복구 필드가 없으면 복구 미설정으로 가입 허용(선택). 단, v2 프론트는 항상 함께 보낸다.

### 5.2 복구용 salt 조회 (신규) — `GET /api/recovery-salt?username=...`
- **출력**: `{ recovery_salt }` / 미설정이면 `404 { error: "복구 키가 설정되지 않은 계정입니다." }`

### 5.3 계정 복구·비번 재설정 (신규) — `POST /api/recover`
- **입력**: `{ username, recovery_auth_key, new_salt, new_auth_key, new_enc_priv, new_enc_priv_iv }`
- **처리**:
  1. `bcrypt.checkpw(recovery_auth_key, recovery_hash)` 실패 시 `401`.
  2. 성공 시 `salt, auth_hash(=bcrypt(new_auth_key)), enc_priv, enc_priv_iv`를 새 값으로 **갱신**(트랜잭션).
- **출력**: `{ ok: true }`
- **주의**: 이 엔드포인트는 `encPrivRecovery`를 **먼저 내려주지 않는다.** 클라이언트는 5.4로 그것을 받아 개인키를 복원한 뒤, 새 봉인값을 만들어 이 엔드포인트로 보낸다. (아래 5.4와 순서 주의)

### 5.4 복구용 개인키 사본 조회 (신규) — `POST /api/recovery-priv`
- **입력**: `{ username, recovery_auth_key }`
- **처리**: `bcrypt.checkpw` 통과해야만 `enc_priv_recovery`(+iv) 반환. 실패 시 `401`.
- **출력**: `{ enc_priv_recovery, enc_priv_recovery_iv }`
- **설계 이유**: 복구 키를 아는 사람에게만 개인키 암호문을 준다. (검증 없이 주면 아무나 암호문을 수집할 수 있으므로)

> **복구 전체 순서**: `GET /api/recovery-salt` → (클라 유도) → `POST /api/recovery-priv`(검증+암호문 수령) → (클라: 개인키 복원 + 새 비번으로 재봉인) → `POST /api/recover`(갱신).

### 5.5 복구 키 재발급 (신규, 인증 필요) — `POST /api/recovery-key/reset`
- **입력**: `{ recovery_salt, recovery_auth_key, enc_priv_recovery, enc_priv_recovery_iv }` (로그인 세션 필요)
- **처리**: 세션 사용자의 복구 관련 컬럼 4개를 덮어씀.
- **출력**: `{ ok: true }`

---

## 6. 프론트 암호 모듈 계약 (`frontend/js/암호.js` 추가 함수)

```javascript
// 복구 키 생성: 256비트 무작위 → 사람이 읽는 그룹 문자열 + 원본 바이트
function generateRecoveryKey() { ... }
// → { display: "XXXX-XXXX-...(8그룹)", keyBytes: Uint8Array(32) }

// 복구 키 → 2갈래 유도 (마스터 비번 deriveKeys와 대칭)
async function deriveRecoveryKeys(recoveryKeyBytesOrDisplay, recoverySaltB64) { ... }
// → { recoveryAuthKey(base64), recoveryWrapKey(CryptoKey) }

// 개인키를 recoveryWrapKey로 봉인 / 해제 (기존 wrap/unwrapPrivateKey 재사용 가능)
//   → wrapPrivateKey(privateKey, recoveryWrapKey), unwrapPrivateKey(...)를 그대로 씀
```
- **재사용**: 개인키 봉인/해제는 기존 `wrapPrivateKey`/`unwrapPrivateKey`가 대칭키만 받으면 되므로 그대로 쓴다. 신규는 복구 키 생성·파싱·유도 함수뿐.
- **표시 형식**: `keyBytes`(32B)를 Base32(사람이 읽기 쉬움, 대문자+숫자)로 인코딩해 4자씩 8그룹 → 하이픈 연결. 입력 시 하이픈·소문자·공백을 정규화해 다시 바이트로 복원.

---

## 7. UI 변경

1. **회원가입 완료 화면**: "복구 키가 발급되었습니다" 모달 → 복구 키 표시 + [복사] 버튼 + "안전한 곳에 보관했습니다" 체크박스(체크해야 닫힘) + ⚠️ "이 키는 다시 표시되지 않습니다" 경고.
2. **로그인 화면**: "비밀번호를 잊으셨나요? → 복구 키로 재설정" 링크 추가 → 복구 화면 진입.
3. **복구 화면(신규)**: `username` + 복구 키 입력 → 검증 후 **새 마스터 비번** 입력 → 완료 시 로그인 화면으로.
4. **금고 화면(선택)**: "복구 키 재발급" 버튼(3.3).

---

## 8. 마이그레이션 (기존 데이터 보존)

- 기존 `금고.db`의 사용자는 복구 컬럼이 NULL. **데이터 손실 없음.**
- `init_db()`가 실행될 때 `PRAGMA table_info(users)`로 컬럼 존재를 확인하고, 없으면 `ALTER TABLE`로 추가만 한다(기존 행은 NULL 유지).
- 기존 사용자는 로그인 후 "복구 키가 설정되지 않았습니다 — 지금 설정하기"(3.3 흐름) 안내를 받는다. **강제하지 않음**(v1 계정도 계속 동작).
- 원본 DB는 변경 전 백업을 권장(파일 복사 1회).

---

## 9. 완료(수용) 기준 & 테스트 계획

### 9.1 수용 기준
- [ ] 신규 가입 시 복구 키가 1회 표시되고, DB에 `recovery_salt/recovery_hash/enc_priv_recovery`가 채워진다.
- [ ] 마스터 비번을 "잊은" 상황을 가정해, **복구 키로 새 비번 설정 → 새 비번으로 로그인 → 기존 시크릿 전부 복호화 성공**.
- [ ] 복구 후에도 **공유받은 시크릿까지 그대로 열림**(개인키 불변 확인).
- [ ] **틀린 복구 키**로는 `POST /api/recovery-priv`가 401, 설령 암호문을 얻어도 복호화 실패.
- [ ] DB를 직접 열어 **복구 키 원본·개인키 평문이 없음**(암호문·bcrypt 해시만) 확인.
- [ ] 기존(복구 키 없는) 계정도 로그인·사용에 지장 없음(마이그레이션 무손실).

### 9.2 테스트 방법
- 기존 `공유암호화_증거테스트.mjs`와 동일한 방식으로 `복구키_증거테스트.mjs`를 추가:
  1. 가입(복구 키 수령) → 시크릿 저장.
  2. "비번 분실" 시나리오: 복구 키로 recovery-priv 수령 → 개인키 복원 → 새 비번으로 recover.
  3. 새 비번으로 로그인 → 시크릿 복호화 일치 확인.
  4. 틀린 복구 키 → 401 및 복호화 실패 확인.
  5. DB 파일 grep으로 복구 키·평문 부재 확인.

---

## 10. 구현 순서 (의존성 그래프)

1. `backend/schema.sql` + `db.py` 마이그레이션(컬럼 추가) — 독립
2. `backend/routes/인증.py`: register 확장 + recovery-salt/recovery-priv/recover/recovery-key reset — db 의존
3. `frontend/js/암호.js`: 복구 키 생성·유도 함수 추가 — 독립
4. `frontend/js/api.js`: 신규 엔드포인트 래퍼 — API 계약 의존
5. `frontend/js/앱.js` + `index.html`: 복구 키 모달·복구 화면 — 위 전부 의존
6. `복구키_증거테스트.mjs`: 종단간 검증
7. `README.md`/`📘팀비밀금고_PRD.md` 갱신(예고 → 구현 완료로)

---

## 11. 비목표 (이번 증분에서 안 하는 것)
- 복구 키 조각 분산(Shamir), 관리자 에스크로, 이메일 기반 복구 — 개념만 README에 언급.
- 복구 시도 횟수 제한(rate limit) — 실습 범위 밖(복구 키가 256비트라 무차별 대입은 비현실적).
